<%= form_for(@song) do |f| %>
  <%= f.label(:title) %><br>
  <%= f.text_field(:title) %><br><br>

  <%= f.label(:artist_name) %><br>
  <%= display_name(@song, @path) %>
  <%= artist_select(@song, @path) %>

  <br><br>
  <%= f.submit %>
<% end %>